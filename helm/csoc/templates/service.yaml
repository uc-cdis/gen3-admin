apiVersion: v1
kind: Service
metadata:
  name: {{ include "csoc.fullname" . }}
  labels:
    {{- include "csoc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "csoc.selectorLabels" . | nindent 4 }}
