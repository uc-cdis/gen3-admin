apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "csoc.fullname" . }}-test-connection"
  labels:
    {{- include "csoc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "csoc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
