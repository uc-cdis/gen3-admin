# Default values for csoc.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  pullPolicy: IfNotPresent
  api:
    repository: quay.io/cdis/csoc-api
    # This sets the pull policy for images.
    # Overrides the image tag whose default is the chart appVersion.
    tag: "master"
  frontend:
    repository: quay.io/cdis/csoc-frontend
    # This sets the pull policy for images.
    # Overrides the image tag whose default is the chart appVersion.
    tag: "master"

# This is for the secretes for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

api:
  workingDir: /go/src/api
  env:
    MOCK_AUTH: "true"
  ports:
    http: 8002
    grpc: 50051
  livenessProbe:
    httpGet:
      path: /ping
      port: 8002
    initialDelaySeconds: 3
  resources:
    limits:
      memory: "512Mi"
      cpu: "1"
    requests:
      memory: "64Mi"
      cpu: "250m"
  volumeMounts:
    - name: data-volume
      mountPath: /go/src/api/certs
    # - name: config-volume
    #   mountPath: /go/src/api/.env
    #   subPath: .env

frontend:
  ports:
    http: 3000
  livenessProbe:
    httpGet:
      path: /
      port: 3000
    initialDelaySeconds: 3
    periodSeconds: 3
  env:
    NEXTAUTH_URL: "https://csoc.planx-pla.net"
    NEXTAUTH_SECRET: "secret" #pragma: allowlist secret
    NEXTAUTH_JWT_SECRET: "secret" #pragma: allowlist secret
  resources:
    limits:
      memory: "512Mi"
      cpu: "500m"
    requests:
      memory: "64Mi"
      cpu: "250m"

persistence:
  enabled: true

  name: csoc-pvc

  accessModes:
    - ReadWriteOnce

  size: 5Gi

  storageClassName: ""   # empty = default storage class

  annotations: {}
#This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

# RBAC — ClusterRole + ClusterRoleBinding for the API pod
rbac:
  create: true

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  type: ClusterIP

  grpc:
    port: 50051      # Service port
    targetPort: 50051  # api containerPort
    type: LoadBalancer

  frontend:
    port: 80          # Service port
    targetPort: 3000  # frontend containerPort

  api:
    port: 8002        # Service port
    targetPort: 8002  # api containerPort

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: true
  className: "alb"
  annotations:
    alb.ingress.kubernetes.io/scheme: internal
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:us-east-1:433568766270:certificate/8cd01903-a34d-41c3-9800-34584e8e32c7
  hosts:
    - host: csoc.planx-pla.net
      paths:
        - path: /api/auth
          pathType: Prefix
          backend:
            service:
              name: csoc
              port:
                name: frontend
        - path: /api/loki
          pathType: Prefix
          backend:
            service:
              name: csoc
              port:
                name: frontend

        - path: /api
          pathType: Prefix
          backend:
            service:
              name: csoc
              port:
                name: api

        - path: /
          pathType: Prefix
          backend:
            service:
              name: csoc
              port:
                name: frontend
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:
  httpGet:
    path: /
    port: http
readinessProbe:
  httpGet:
    path: /
    port: http

#This section is for setting up autoscaling more information can be found here: https://kubernetes.io/docs/concepts/workloads/autoscaling/
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes:
  config:
    name: config-volume
    configMapName: csoc
  data:
    name: data-volume
    pvcName: csoc-pvc
# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}
